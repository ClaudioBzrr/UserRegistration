import { Box, Flex, HStack } from "@chakra-ui/react";
import { Link } from "react-router-dom";


export function Header(){
    return(
        <Box bg={'white'} position={"fixed"} top={0}>
            <Flex boxShadow={'lg'} minW={'100vw'} h={16} justify={'space-between'} align={'center'}>
                    
            </Flex>
        </Box>
    )
}